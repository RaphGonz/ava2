---
phase: 14-apply-the-style-of-the-front-page-to-the-rest-of-the-ui-of-the-entire-app
plan: "04"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/BillingPage.tsx
  - frontend/src/pages/SubscribePage.tsx
  - frontend/src/pages/AvatarSetupPage.tsx
  - frontend/src/pages/AdminPage.tsx
  - frontend/src/components/landing/LandingHero.tsx
autonomous: false
requirements:
  - UI-05
  - UI-06

must_haves:
  truths:
    - "SettingsPage uses bg-black outer, all 5 sections wrapped in GlassCard, active selectors use gradient highlight, new Subscription section added at bottom with plan status + Manage Billing link + Sign out button"
    - "BillingPage uses bg-black (was bg-gray-950) with a Back to Settings button"
    - "SubscribePage uses bg-black outer, GlassCard inner, feature list uses bg-white/5 border border-white/10, primary button uses gradient"
    - "AvatarSetupPage uses bg-black outer, GlassCard inner, dark inputs, gradient buttons, spinner uses border-violet-500"
    - "AdminPage uses bg-black (was bg-gray-950) — GlassCards already present, no other changes needed"
    - "LandingHero right mockup shows chat bubbles + locked blurred photo instead of audio visualizer"
    - "LandingPage.test.tsx stays GREEN after LandingHero change"
  artifacts:
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "Dark settings page with GlassCard sections and Subscription section + Sign out"
      contains: "bg-black"
    - path: "frontend/src/pages/BillingPage.tsx"
      provides: "Pure black billing page with back button to Settings"
      contains: "bg-black"
    - path: "frontend/src/pages/SubscribePage.tsx"
      provides: "Dark subscribe page with GlassCard and gradient button"
      contains: "bg-black"
    - path: "frontend/src/pages/AvatarSetupPage.tsx"
      provides: "Dark onboarding page with GlassCard and gradient buttons"
      contains: "bg-black"
    - path: "frontend/src/pages/AdminPage.tsx"
      provides: "Pure black admin page"
      contains: "bg-black"
    - path: "frontend/src/components/landing/LandingHero.tsx"
      provides: "Updated right mockup with chat bubbles + locked photo"
      contains: "from-blue-600"
  key_links:
    - from: "frontend/src/pages/SettingsPage.tsx"
      to: "/billing"
      via: "Subscription section Link to='/billing' — Manage Billing button"
      pattern: "Link.*billing"
    - from: "frontend/src/pages/BillingPage.tsx"
      to: "/settings"
      via: "Back button using Link or useNavigate to /settings"
      pattern: "settings"
    - from: "frontend/src/components/landing/LandingHero.tsx"
      to: "LandingPage.test.tsx"
      via: "Test renders LandingPage which renders LandingHero — must not crash"
      pattern: "LandingPage.test"
---

<objective>
Restyle the remaining authenticated pages (SettingsPage, BillingPage, SubscribePage, AvatarSetupPage, AdminPage) and update the LandingHero right mockup. Includes adding a Subscription section + Sign out to SettingsPage and a back button to BillingPage.

Purpose: Completes the visual unification pass. After this plan, every page in the app uses the same dark glass visual language as the landing page.

Output: 6 restyled files. SettingsPage gets a new Subscription section and Sign out button. BillingPage gets a back button.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-apply-the-style-of-the-front-page-to-the-rest-of-the-ui-of-the-entire-app/14-CONTEXT.md
@.planning/phases/14-apply-the-style-of-the-front-page-to-the-rest-of-the-ui-of-the-entire-app/14-RESEARCH.md

<interfaces>
<!-- Key patterns for the executor. -->

Active selector gradient (from RESEARCH.md):
```typescript
// Replace bg-gray-900 active state with:
className={`py-2 px-3 rounded-xl text-sm border transition-all ${
  isActive
    ? 'bg-gradient-to-r from-blue-600 to-violet-600 border-transparent text-white'
    : 'border-white/10 text-slate-400 hover:border-white/20 hover:text-white bg-white/5'
}`}
```

GlassCard section replacement (from RESEARCH.md):
```typescript
// Before:
<section className="bg-white rounded-2xl p-5 shadow-sm border border-gray-100">
// After:
<GlassCard className="p-5">
  {/* same content */}
</GlassCard>
```

Settings subscription section (new — append before Save button):
```tsx
<GlassCard className="p-5">
  <h2 className="text-sm font-semibold text-white mb-1">Subscription</h2>
  {subscriptionLoading ? (
    <p className="text-slate-400 text-sm">Loading...</p>
  ) : subscription ? (
    <div className="space-y-2">
      <p className="text-sm text-slate-300">
        Plan: <span className="text-white font-medium">{subscription.plan_name ?? 'Ava Monthly'}</span>
      </p>
      <p className={`text-sm ${subscription.status === 'active' ? 'text-green-400' : 'text-yellow-400'}`}>
        {subscription.status.charAt(0).toUpperCase() + subscription.status.slice(1)}
      </p>
    </div>
  ) : (
    <p className="text-sm text-slate-400">No active subscription.</p>
  )}
  <Link
    to="/billing"
    className="mt-3 inline-block text-sm text-violet-400 hover:text-violet-300 transition-colors"
  >
    Manage Billing →
  </Link>
</GlassCard>
```

Sign out button (append at very bottom of SettingsPage, after Save button):
```tsx
<button
  onClick={handleSignOut}
  className="w-full bg-white/5 border border-white/10 text-slate-400 hover:text-white hover:bg-white/10 rounded-xl py-3 text-sm font-medium transition-colors mt-4"
>
  Sign out
</button>
```

BillingPage back button (add at top of content, before the cancel flow):
```tsx
<Link
  to="/settings"
  className="inline-flex items-center gap-2 text-sm text-slate-400 hover:text-white transition-colors mb-8"
>
  ← Back to Settings
</Link>
```

AvatarSetupPage updates:
- `bg-gray-950` → `bg-black` on outer div
- `bg-gray-900 rounded-2xl p-8` inner card → `<GlassCard className="p-8">`
- `bg-gray-800 border border-gray-700` inputs → `bg-white/5 border border-white/10` + `text-white placeholder-gray-500 focus:outline-none focus:border-blue-500/50`
- `bg-purple-600 text-white` personality active button → `bg-gradient-to-r from-blue-600 to-violet-600 text-white`
- `bg-gray-800 text-gray-300` personality inactive → `bg-white/5 text-slate-400 hover:bg-white/10`
- `bg-purple-600 hover:bg-purple-700` submit button → `bg-gradient-to-r from-blue-600 to-violet-600 hover:from-blue-500 hover:to-violet-500`
- `border-purple-600` spinner → `border-violet-500`
- `bg-gray-700 hover:bg-gray-600` secondary buttons (Regenerate, Try again) → `bg-white/5 border border-white/10 hover:bg-white/10`

LandingHero right mockup replacement (inner content only):
- Locate the `bg-white/10 backdrop-blur-xl rounded-xl p-3 mb-4` notification block and the audio visualizer div
- Replace BOTH with the chat bubbles + locked photo content (see below)
- Keep the outer container `w-64 bg-black/40 backdrop-blur-md border border-white/10 rounded-3xl p-4 ...` unchanged

Chat bubbles + locked photo replacement content:
```tsx
{/* Chat bubbles */}
<div className="space-y-2 mb-3">
  {/* Ava message */}
  <div className="bg-white/5 backdrop-blur-sm border border-white/10 rounded-xl rounded-bl-sm px-3 py-2 text-xs text-white/90 text-left max-w-[85%]">
    Missing you already... 🌙
  </div>
  {/* User message */}
  <div className="bg-gradient-to-r from-blue-600 to-violet-600 rounded-xl rounded-br-sm px-3 py-2 text-xs text-white text-right ml-auto max-w-[75%]">
    Show me something
  </div>
  {/* Ava message */}
  <div className="bg-white/5 backdrop-blur-sm border border-white/10 rounded-xl rounded-bl-sm px-3 py-2 text-xs text-white/90 text-left max-w-[85%]">
    Just for you... 💜
  </div>
</div>
{/* Blurred locked photo */}
<div className="relative rounded-xl overflow-hidden h-24">
  <div className="absolute inset-0 bg-gradient-to-b from-violet-800/30 to-orange-800/30 backdrop-blur-lg" />
  <div className="absolute inset-0 flex items-center justify-center">
    <span className="text-white/70 text-xs font-medium">🔒 Click to unlock</span>
  </div>
</div>
```
Also remove the `BAR_HEIGHTS` constant and its import (now unused) from LandingHero.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SettingsPage + BillingPage + SubscribePage + AvatarSetupPage</name>
  <files>
    frontend/src/pages/SettingsPage.tsx,
    frontend/src/pages/BillingPage.tsx,
    frontend/src/pages/SubscribePage.tsx,
    frontend/src/pages/AvatarSetupPage.tsx
  </files>
  <action>
Read each file before modifying. CRITICAL: Only change className values and add new UI sections. Do not change any logic, state management, API calls, or existing functionality.

**SettingsPage.tsx:**
1. Add imports: `import { GlassCard } from '../components/ui/GlassCard'`, `import { Link } from 'react-router-dom'`, `import { getSubscription } from '../api/billing'`, `import { useQuery } from '@tanstack/react-query'`, `import { signOut } from '../api/auth'`
2. Add subscription query inside component (after existing `token` declaration):
   ```typescript
   const { data: subscription, isLoading: subscriptionLoading } = useQuery({
     queryKey: ['subscription'],
     queryFn: () => getSubscription(token!),
     staleTime: 60 * 1000,
     enabled: !!token,
   })
   ```
3. Add `handleSignOut` function:
   ```typescript
   async function handleSignOut() {
     await signOut()
     useAuthStore.getState().clearAuth()
     navigate('/login')
   }
   ```
   Note: `clearAuth` comes from `useAuthStore`. Check existing imports — `useAuthStore` is already imported. `signOut` is from `'../api/auth'` (same as ChatPage pattern). `navigate` is already imported from `useNavigate`.
4. Outer div: `min-h-screen bg-gray-50` → `min-h-screen bg-black`
5. Loading state div: `min-h-screen flex items-center justify-center` → `min-h-screen flex items-center justify-center bg-black`, `text-gray-400` → `text-slate-400`
6. Header back button: `text-gray-400 hover:text-gray-700` → `text-slate-400 hover:text-white`, arrow text unchanged
7. Header h1: `text-gray-900` → `text-white`
8. Replace all 5 `<section className="bg-white rounded-2xl p-5 shadow-sm border border-gray-100">` with `<GlassCard className="p-5">`
9. Inside each section, update text colors: `text-gray-900` → `text-white`, `text-gray-500` → `text-slate-400`
10. Persona buttons — apply active selector gradient pattern (see interfaces above)
11. Platform buttons — apply active selector gradient pattern
12. Spiciness buttons — apply active selector gradient pattern (same logic, full-width buttons)
13. Mode-switch phrase input: apply dark glass input pattern
14. Notification section label: `text-gray-700` → `text-slate-300`
15. Save button: `bg-gray-900 text-white rounded-xl py-3 ... hover:bg-gray-700` → `bg-gradient-to-r from-blue-600 to-violet-600 hover:from-blue-500 hover:to-violet-500 text-white rounded-xl py-3 text-sm font-medium transition-all disabled:opacity-50`
16. Error: `text-red-500` stays fine; success: `text-green-600` → `text-green-400`
17. Add Subscription GlassCard section BEFORE the Save button (see interfaces above for full markup)
18. Add Sign out button AFTER the Save button (see interfaces above)

**BillingPage.tsx:**
1. `bg-gray-950` → `bg-black` on the outermost div
2. Add `import { Link } from 'react-router-dom'` if not already imported (it is already imported)
3. Add back button before the `{cancelStep !== 'idle' && ...}` block:
   ```tsx
   <Link
     to="/settings"
     className="inline-flex items-center gap-2 text-sm text-slate-400 hover:text-white transition-colors mb-8"
   >
     ← Back to Settings
   </Link>
   ```
4. Billing section buttons: update primary `bg-violet-600` → `bg-gradient-to-r from-blue-600 to-violet-600` (the two main action buttons: "Manage billing" and "Upgrade"). Keep cancel/danger buttons (red) unchanged.

**SubscribePage.tsx:**
1. Add imports: `import { GlassCard } from '../components/ui/GlassCard'`
2. Outer: `bg-gray-950` → `bg-black`
3. Inner card: `w-full max-w-sm bg-gray-900 rounded-2xl p-8 space-y-6 text-center` → replace with `motion.div + GlassCard`:
   ```tsx
   <div className="w-full max-w-sm">
     <GlassCard className="p-8 text-center space-y-6">
       {/* all content */}
     </GlassCard>
   </div>
   ```
   (motion entrance is optional here — keep it simple)
4. Heading: `text-2xl font-bold` (text is white by default inside GlassCard dark bg)
5. Sub-text: `text-gray-400` stays fine
6. Feature list box: `bg-gray-800 rounded-xl p-4` → `bg-white/5 border border-white/10 rounded-xl p-4`
7. Feature list text: `text-gray-400` stays fine
8. Primary button: `bg-purple-600 hover:bg-purple-700` → `bg-gradient-to-r from-blue-600 to-violet-600 hover:from-blue-500 hover:to-violet-500`

**AvatarSetupPage.tsx:** Apply all changes listed in the interfaces section above. Read the file first to confirm exact className strings. The logic (form state, API calls, polling) must not change.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 files compile cleanly. SettingsPage has GlassCard sections, subscription section, and sign out button. BillingPage has bg-black and back button. SubscribePage has GlassCard and gradient button. AvatarSetupPage has bg-black, GlassCard, gradient buttons.</done>
</task>

<task type="auto">
  <name>Task 2: AdminPage bg-black + LandingHero mockup replacement</name>
  <files>
    frontend/src/pages/AdminPage.tsx,
    frontend/src/components/landing/LandingHero.tsx
  </files>
  <action>
**AdminPage.tsx:**
Read the file first. The page already uses GlassCard and is nearly correct.
1. Find the outer page div — change `bg-gray-950` to `bg-black`
2. No other changes needed — GlassCards are already present

**LandingHero.tsx:**
This is a targeted surgical replacement. The outer `w-64 bg-black/40` container stays unchanged.
1. Remove the `BAR_HEIGHTS` constant at the top of the file
2. Locate the notification block: `<div className="bg-white/10 backdrop-blur-xl rounded-xl p-3 mb-4 border border-white/5 relative overflow-hidden group">` — remove this entire div and its children
3. Locate the audio visualizer: `<div className="flex items-center justify-center gap-1 h-8 mt-8 opacity-60">` with the BAR_HEIGHTS.map — remove this entire div
4. In the space where those two elements were removed, insert the chat bubbles + locked photo content from the interfaces section above

The outer mockup container and its shadow/gradient bar must remain unchanged. Only the inner content changes.

CRITICAL: After editing LandingHero.tsx, run `cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm test -- --run LandingPage` to confirm LandingPage.test.tsx still passes GREEN. If it fails, check that no component names or exports were accidentally changed.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm test -- --run LandingPage 2>&1 | tail -20</automated>
  </verify>
  <done>AdminPage uses bg-black. LandingHero shows chat bubbles + locked photo. LandingPage.test.tsx stays GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of full dark glass redesign</name>
  <what-built>
    Full dark glass visual restyling across all app pages:
    - Plan 01: AppNav (bottom/top nav) + AuthenticatedLayout in App.tsx
    - Plan 02: Auth pages (login, signup, forgot-password, reset-password) — black bg, GlassCard, gradient buttons
    - Plan 03: Chat (ChatPage dark header with avatar, gradient bubbles, glass Ava bubbles, dark input)
    - Plan 04: Settings (GlassCard sections, subscription section, sign out), Billing (back button), Subscribe (GlassCard), AvatarSetup (GlassCard), Admin (bg-black), LandingHero (chat bubbles mockup)
  </what-built>
  <how-to-verify>
    1. Open the app in browser at http://localhost:5173 (run `cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm run dev` first if needed)

    2. Landing page (/):
       - Right mockup shows chat bubbles ("Missing you already...", "Show me something", "Just for you...") and a blurred locked photo — NOT audio visualizer bars
       - Left Jarvis mockup unchanged
       - Page still renders without visual errors

    3. Login page (/login):
       - Black background
       - Centered glassmorphism card (frosted glass look)
       - Gradient "Sign in" button (blue to violet)
       - Dark text inputs

    4. Sign up page (/signup): same dark glass appearance as login

    5. Chat page (/chat — sign in first):
       - Full-width dark black background (no max-width card)
       - Header: gradient "Ava" name text, green pulsing online dot, avatar image or gradient circle
       - NO Settings/Sign out buttons in header
       - Bottom tab bar visible on mobile OR top nav visible on desktop (Chat | Photos | Settings)

    6. Settings page (/settings):
       - Black background
       - Each section is a frosted GlassCard (Persona, Platform, Content Ceiling, Mode-Switch Phrase, Notifications)
       - Active selection shows blue-violet gradient highlight
       - Subscription section at the bottom (plan name, status, Manage Billing link)
       - Sign out button at the very bottom

    7. Billing page (/billing):
       - "← Back to Settings" link at top
       - Pure black background (was dark gray)

    8. Subscribe page (/subscribe):
       - Black background, GlassCard, blue-violet gradient subscribe button

    9. Avatar setup page (/avatar-setup — only visible during first onboarding):
       - Black background, GlassCard form, gradient "Create Ava" button

    10. Navigation: clicking Chat/Photos/Settings tabs navigates correctly. Tab bar does NOT appear on landing, login, signup pages.
  </how-to-verify>
  <resume-signal>Type "approved" if visual review passes, or describe any issues to fix</resume-signal>
  <action>Human visual review — see how-to-verify steps above</action>
  <verify><automated>echo "human checkpoint"</automated></verify>
  <done>User types "approved" after visual review passes</done>
</task>

</tasks>

<verification>
Run full suite: `cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm test -- --run`
All tests must be GREEN:
- LandingPage.test.tsx
- AdminPage.test.tsx
- AppNav.test.tsx
- ChatBubble.test.tsx
- LoginPage.test.tsx

Run build: `cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm run build` — zero errors

Human visual verification required (checkpoint above).
</verification>

<success_criteria>
- SettingsPage: bg-black, 5 GlassCard sections, gradient active selectors, Subscription section + Sign out button
- BillingPage: bg-black (was bg-gray-950), "← Back to Settings" button present
- SubscribePage: bg-black, GlassCard, gradient button (no more bg-purple-600)
- AvatarSetupPage: bg-black, GlassCard inner, dark inputs, gradient buttons, border-violet-500 spinner
- AdminPage: bg-black (was bg-gray-950)
- LandingHero: chat bubbles + locked photo mockup (no audio bars, no BAR_HEIGHTS)
- All 5 tests GREEN
- TypeScript build clean
- Human visual approval received
</success_criteria>

<output>
After completion, create `.planning/phases/14-apply-the-style-of-the-front-page-to-the-rest-of-the-ui-of-the-entire-app/14-04-SUMMARY.md`
</output>
