---
phase: 10-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/components/ui/GlassCard.tsx
  - frontend/src/pages/LandingPage.test.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
must_haves:
  truths:
    - "GlassCard component is importable from frontend/src/components/ui/GlassCard.tsx with all three variants"
    - "motion, lucide-react, clsx, tailwind-merge are installed as production dependencies"
    - "vitest test scaffold exists covering LAND-01, LAND-02, LAND-03 behaviors (failing until components built)"
  artifacts:
    - path: "frontend/src/components/ui/GlassCard.tsx"
      provides: "Shared glass card primitive used by DualPromise, Trust, Pricing sections"
      exports: ["GlassCard"]
    - path: "frontend/vitest.config.ts"
      provides: "Test framework configuration"
      contains: "jsdom"
    - path: "frontend/src/pages/LandingPage.test.tsx"
      provides: "Test scaffold for all three LAND requirements"
      contains: "LAND-01, LAND-02, LAND-03"
  key_links:
    - from: "frontend/src/components/landing/*.tsx"
      to: "frontend/src/components/ui/GlassCard.tsx"
      via: "named import"
      pattern: "from.*components/ui/GlassCard"
---

<objective>
Install the four required npm packages, port the GlassCard shared component from the Figma export, configure vitest for frontend testing, and create a failing test scaffold that will turn green once all components are implemented.

Purpose: Establish the shared foundation (GlassCard + deps) that all landing section components will import from, and create the test scaffold that enforces LAND-01/02/03 correctness at the end of the phase.
Output: Installed deps, GlassCard.tsx, vitest.config.ts, LandingPage.test.tsx (failing at this stage — expected).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-landing-page/10-CONTEXT.md
@.planning/phases/10-landing-page/10-RESEARCH.md

<interfaces>
<!-- Figma GlassCard source (port verbatim): -->
<!-- .planning/assets/figma_frames/src/app/components/ui/GlassCard.tsx -->
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";
import { motion, HTMLMotionProps } from "motion/react";

function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

interface GlassCardProps extends HTMLMotionProps<"div"> {
  variant?: "base" | "active-warm" | "active-cool";
  children: React.ReactNode;
  className?: string;
}

export function GlassCard({ variant = "base", children, className, ...props }: GlassCardProps) {
  const variants = {
    base: "bg-white/5 border-white/10 hover:border-white/20 hover:bg-white/10",
    "active-warm": "bg-orange-500/10 border-orange-500/30 shadow-[0_0_30px_-5px_rgba(249,115,22,0.3)]",
    "active-cool": "bg-blue-500/10 border-blue-500/30 shadow-[0_0_30px_-5px_rgba(59,130,246,0.3)]",
  };
  return (
    <motion.div
      className={cn("backdrop-blur-md border rounded-2xl p-6 transition-all duration-300", variants[variant], className)}
      {...props}
    >
      {children}
    </motion.div>
  );
}
```

<!-- Auth store interface (for test mocking): -->
<!-- frontend/src/store/useAuthStore.ts -->
<!-- token: string | null is the auth signal; non-null = authenticated -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure vitest</name>
  <files>frontend/package.json, frontend/vitest.config.ts</files>
  <action>
Run from the repo root:
```
cd frontend && npm install motion lucide-react clsx tailwind-merge
cd frontend && npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom @testing-library/user-event
```

Then add a `"test"` script to `frontend/package.json`:
```json
"test": "vitest"
```

Create `frontend/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/setupTests.ts'],
  },
})
```

Create `frontend/src/setupTests.ts`:
```typescript
import '@testing-library/jest-dom'
```

Verify vitest can run (it will find no test files yet — that is acceptable at this step):
```bash
cd frontend && npm run test -- --run 2>&1 | grep -E "(passed|failed|no test files|PASS|FAIL|Error)" | head -5
```
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2/frontend && node -e "require('./node_modules/motion/react')" 2>/dev/null && echo "motion OK" && node -e "require('./node_modules/lucide-react')" 2>/dev/null && echo "lucide OK" && node -e "require('./node_modules/clsx')" 2>/dev/null && echo "clsx OK" && node -e "require('./node_modules/tailwind-merge')" 2>/dev/null && echo "tw-merge OK"</automated>
  </verify>
  <done>All four packages appear in node_modules; vitest.config.ts exists; package.json has "test": "vitest" script.</done>
</task>

<task type="auto">
  <name>Task 2: Port GlassCard component from Figma export</name>
  <files>frontend/src/components/ui/GlassCard.tsx</files>
  <action>
Create `frontend/src/components/ui/GlassCard.tsx` by porting verbatim from the Figma source (`.planning/assets/figma_frames/src/app/components/ui/GlassCard.tsx`).

The file is reproduced exactly as-is — no changes needed. The three variants (`base`, `active-warm`, `active-cool`) are unchanged. The `cn()` helper using `clsx` + `tailwind-merge` lives inside the file (not exported).

Key: imports use `"motion/react"` (Framer Motion v12), NOT `"framer-motion"`.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2 && grep -c "motion/react" frontend/src/components/ui/GlassCard.tsx && grep -c "active-warm" frontend/src/components/ui/GlassCard.tsx && echo "GlassCard OK"</automated>
  </verify>
  <done>GlassCard.tsx exists at frontend/src/components/ui/GlassCard.tsx, exports GlassCard function, imports from "motion/react", contains all three variant strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create failing test scaffold for LandingPage requirements</name>
  <files>frontend/src/pages/LandingPage.test.tsx</files>
  <behavior>
    - LAND-01: Rendered page contains elements with test ids or text for hero, features (DualPromise/Trust), and pricing sections
    - LAND-02: Primary CTA buttons render as links with href="/signup" (or render-to attribute "/signup" via React Router)
    - LAND-03: Rendered HTML output does NOT contain the strings "intimate", "explicit", "NSFW", "adult", "18+", "intimité", "Créer", "Gratuit", "mois", "Maintenant", "Compagnon" (French), "ShieldAlert"
  </behavior>
  <action>
Create `frontend/src/pages/LandingPage.test.tsx`. This test will FAIL at this stage because LandingPage is still the placeholder. That is expected and correct (RED phase of TDD).

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import LandingPage from './LandingPage'

// Mock useAuthStore to return unauthenticated state (no token)
vi.mock('../store/useAuthStore', () => ({
  useAuthStore: (selector: (s: { token: null }) => unknown) =>
    selector({ token: null }),
}))

function renderLanding() {
  return render(
    <MemoryRouter initialEntries={['/']}>
      <LandingPage />
    </MemoryRouter>
  )
}

describe('LandingPage — LAND-01: Required sections present', () => {
  it('renders a hero section', () => {
    renderLanding()
    // Hero section is identifiable by the primary headline text
    expect(screen.getByText(/The AI that takes care of your day/i)).toBeInTheDocument()
  })

  it('renders a features/DualPromise section', () => {
    renderLanding()
    expect(screen.getByText(/Assistant Mode/i)).toBeInTheDocument()
    expect(screen.getByText(/Companion Mode/i)).toBeInTheDocument()
  })

  it('renders a pricing section', () => {
    renderLanding()
    expect(screen.getByText(/Choose your plan/i)).toBeInTheDocument()
    expect(screen.getByText(/Premium/i)).toBeInTheDocument()
  })
})

describe('LandingPage — LAND-02: CTAs link to /signup', () => {
  it('primary CTA links to /signup', () => {
    renderLanding()
    const ctaLinks = screen.getAllByRole('link', { name: /Create my Ava/i })
    expect(ctaLinks.length).toBeGreaterThan(0)
    expect(ctaLinks[0]).toHaveAttribute('href', '/signup')
  })

  it('Get started link routes to /signup', () => {
    renderLanding()
    const links = screen.getAllByRole('link', { name: /Get started/i })
    expect(links.length).toBeGreaterThan(0)
    links.forEach(link => expect(link).toHaveAttribute('href', '/signup'))
  })
})

describe('LandingPage — LAND-03: No prohibited copy', () => {
  it('contains no prohibited NSFW or adult language', () => {
    const { container } = renderLanding()
    const html = container.innerHTML

    const prohibited = [
      'intimate', 'explicit', 'NSFW', 'adult', '18+',
      'intimité', 'Créer', 'mois', 'ShieldAlert',
    ]
    prohibited.forEach(word => {
      expect(html).not.toContain(word)
    })
  })

  it('contains no French copy', () => {
    const { container } = renderLanding()
    const html = container.innerHTML

    const frenchWords = ['Gratuit', 'Maintenant', 'Choisissez', 'Confiance', 'Démoriser']
    frenchWords.forEach(word => {
      expect(html).not.toContain(word)
    })
  })
})

describe('LandingPage — Auth redirect', () => {
  it('redirects authenticated users away from landing page', () => {
    vi.mock('../store/useAuthStore', () => ({
      useAuthStore: (selector: (s: { token: string }) => unknown) =>
        selector({ token: 'fake-token' }),
    }))
    // With a token, the Navigate component renders; landing content does not
    const { container } = render(
      <MemoryRouter initialEntries={['/']}>
        <LandingPage />
      </MemoryRouter>
    )
    // Hero headline should NOT be present for authenticated user
    expect(container.innerHTML).not.toContain('The AI that takes care')
  })
})
```

After writing the file, run tests to confirm they fail (RED):
```bash
cd frontend && npm run test -- --run src/pages/LandingPage.test.tsx 2>&1 | tail -10
```
The output should show failures — this is correct.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm run test -- --run src/pages/LandingPage.test.tsx 2>&1 | grep -E "(FAIL|failed|Error)" | head -3</automated>
  </verify>
  <done>LandingPage.test.tsx exists. Running tests produces FAIL output (expected RED phase — placeholder LandingPage does not yet implement the Figma design). Tests will turn GREEN after Plans 02–04 complete.</done>
</task>

</tasks>

<verification>
- `ls frontend/src/components/ui/GlassCard.tsx` → file exists
- `ls frontend/vitest.config.ts` → file exists
- `ls frontend/src/pages/LandingPage.test.tsx` → file exists
- `cat frontend/package.json | grep '"motion"'` → shows `"motion"` in dependencies
- `cat frontend/package.json | grep '"test"'` → shows vitest script
- Tests fail (expected): `cd frontend && npm run test -- --run src/pages/LandingPage.test.tsx` exits non-zero
</verification>

<success_criteria>
- motion, lucide-react, clsx, tailwind-merge installed in frontend/node_modules
- vitest + @testing-library/react configured and runnable
- GlassCard.tsx ported from Figma source, exports GlassCard with 3 variants
- LandingPage.test.tsx covers all LAND-01/02/03 behaviors and is in RED (failing) state
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-01-SUMMARY.md`
</output>
