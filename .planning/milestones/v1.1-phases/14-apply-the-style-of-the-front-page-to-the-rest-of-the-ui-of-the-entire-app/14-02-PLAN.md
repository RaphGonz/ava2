---
phase: 14-apply-the-style-of-the-front-page-to-the-rest-of-the-ui-of-the-entire-app
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/SignupPage.tsx
  - frontend/src/pages/ForgotPasswordPage.tsx
  - frontend/src/pages/ResetPasswordPage.tsx
  - frontend/src/components/GoogleSignInButton.tsx
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "Login, signup, forgot-password, and reset-password pages all render on a pure black background"
    - "Each auth page has a centered glassmorphism card (GlassCard or equivalent) with motion entrance animation"
    - "All form inputs use dark glass style: bg-white/5 border border-white/10 text-white focus:ring-blue-500/50"
    - "Primary submit buttons use gradient from-blue-600 to-violet-600"
    - "GoogleSignInButton renders with dark border/text on black background (border-white/20 text-white hover:bg-white/5)"
    - "LoginPage test (scaffold from Plan 01) turns GREEN — bg-black class present"
  artifacts:
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Dark-themed login page with GlassCard, gradient button"
      contains: "bg-black"
    - path: "frontend/src/pages/SignupPage.tsx"
      provides: "Dark-themed signup page — same pattern as LoginPage"
      contains: "bg-black"
    - path: "frontend/src/pages/ForgotPasswordPage.tsx"
      provides: "Dark-themed forgot-password page"
      contains: "bg-black"
    - path: "frontend/src/pages/ResetPasswordPage.tsx"
      provides: "Dark-themed reset-password page (3 states: error, success, form)"
      contains: "bg-black"
    - path: "frontend/src/components/GoogleSignInButton.tsx"
      provides: "Dark-bordered Google button"
      contains: "border-white/20"
  key_links:
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "frontend/src/components/ui/GlassCard.tsx"
      via: "GlassCard import replacing bg-white card div"
      pattern: "GlassCard"
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "frontend/src/components/GoogleSignInButton.tsx"
      via: "GoogleSignInButton usage unchanged — only the component itself is restyled"
      pattern: "GoogleSignInButton"
---

<objective>
Restyle all four auth pages (login, signup, forgot-password, reset-password) and the GoogleSignInButton component to the dark glass visual language.

Purpose: The auth pages are the user's first impression of the app. They should feel like a seamless continuation of the landing page, not a light-themed form.

Output: 5 restyled files. All existing logic preserved — only classNames change.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-apply-the-style-of-the-front-page-to-the-rest-of-the-ui-of-the-entire-app/14-CONTEXT.md
@.planning/phases/14-apply-the-style-of-the-front-page-to-the-rest-of-the-ui-of-the-entire-app/14-RESEARCH.md

<interfaces>
<!-- Patterns to apply uniformly across all auth pages. -->

Dark auth card pattern (from RESEARCH.md):
```typescript
import { motion } from 'motion/react'
import { GlassCard } from '../components/ui/GlassCard'

// Outer wrapper:
<div className="min-h-screen flex items-center justify-center bg-black">
  <motion.div
    initial={{ opacity: 0, y: 20 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.4 }}
    className="w-full max-w-sm"
  >
    <GlassCard className="p-8">
      {/* form content */}
    </GlassCard>
  </motion.div>
</div>
```

Dark input pattern:
```typescript
<input className="w-full bg-white/5 border border-white/10 rounded-lg px-3 py-2 text-sm text-white placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500/50" />
```

Primary button pattern:
```typescript
<button className="w-full bg-gradient-to-r from-blue-600 to-violet-600 hover:from-blue-500 hover:to-violet-500 text-white rounded-lg py-2 text-sm font-medium transition-all disabled:opacity-50">
```

Divider pattern:
```typescript
<div className="flex-1 h-px bg-white/10" />
<span className="text-xs text-slate-500">or</span>
<div className="flex-1 h-px bg-white/10" />
```

Label pattern:
```typescript
<label className="block text-sm font-medium text-slate-300 mb-1">
```

Link/secondary text pattern:
```typescript
<Link to="/forgot-password" className="text-slate-400 hover:text-white text-sm transition-colors">
```

GoogleSignInButton dark border update (only className changes — logic unchanged):
```typescript
// Replace: border border-gray-200 text-gray-700 hover:bg-gray-50
// With:    border border-white/20 text-white hover:bg-white/5
```

GlassCard import path:
```typescript
import { GlassCard } from '../components/ui/GlassCard'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle LoginPage + SignupPage + GoogleSignInButton</name>
  <files>
    frontend/src/pages/LoginPage.tsx,
    frontend/src/pages/SignupPage.tsx,
    frontend/src/components/GoogleSignInButton.tsx
  </files>
  <action>
CRITICAL: All existing logic, state, event handlers, useEffect hooks, and imports must be preserved unchanged. ONLY modify className strings and add the motion/GlassCard wrapper.

**LoginPage.tsx changes:**
1. Add imports: `import { motion } from 'motion/react'` and `import { GlassCard } from '../components/ui/GlassCard'`
2. Replace outer div `className="min-h-screen flex items-center justify-center bg-gray-50"` with `className="min-h-screen flex items-center justify-center bg-black"`
3. Replace inner card div `className="bg-white rounded-2xl shadow-sm border border-gray-100 p-8 w-full max-w-sm"` with a `motion.div` + `GlassCard` pattern:
   ```tsx
   <motion.div
     initial={{ opacity: 0, y: 20 }}
     animate={{ opacity: 1, y: 0 }}
     transition={{ duration: 0.4 }}
     className="w-full max-w-sm"
   >
     <GlassCard className="p-8">
       {/* all form content goes here */}
     </GlassCard>
   </motion.div>
   ```
4. Heading: `text-gray-900` → `text-white`
5. Subheading: `text-gray-500` → `text-slate-400`
6. Labels: `text-gray-700` → `text-slate-300`
7. Inputs: replace `border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-gray-900` with `bg-white/5 border border-white/10 rounded-lg px-3 py-2 text-sm text-white placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500/50`
8. Divider lines: `bg-gray-100` → `bg-white/10`
9. Divider text: `text-gray-400` → `text-slate-500`
10. Primary button: `bg-gray-900 text-white rounded-lg py-2 text-sm font-medium hover:bg-gray-700 disabled:opacity-50 transition-colors` → `bg-gradient-to-r from-blue-600 to-violet-600 hover:from-blue-500 hover:to-violet-500 text-white rounded-lg py-2 text-sm font-medium transition-all disabled:opacity-50`
11. Footer links: `text-gray-500` → `text-slate-400` and `text-gray-900 font-medium` → `text-white font-medium`

**SignupPage.tsx changes:** Apply the identical set of changes as LoginPage. Read the file first to understand its exact structure. Same pattern: bg-black outer, motion.div + GlassCard inner, dark inputs, gradient button, dark labels/links.

**GoogleSignInButton.tsx changes:**
Read the file first. Locate the button element's className. Update only the styling classes:
- `border border-gray-200` → `border border-white/20`
- `text-gray-700` → `text-white`
- `hover:bg-gray-50` → `hover:bg-white/5`
- Keep the Google SVG logo and all other content completely unchanged.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm test -- --run LoginPage 2>&1 | tail -20</automated>
  </verify>
  <done>LoginPage.test.tsx passes GREEN (bg-black class present). SignupPage and GoogleSignInButton compile without errors. `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ForgotPasswordPage + ResetPasswordPage</name>
  <files>
    frontend/src/pages/ForgotPasswordPage.tsx,
    frontend/src/pages/ResetPasswordPage.tsx
  </files>
  <action>
Read each file first to understand its current structure before modifying.

**ForgotPasswordPage.tsx changes:** Same pattern as LoginPage. The page likely has a single form state.
1. Add imports: `motion` from `'motion/react'`, `GlassCard` from `'../components/ui/GlassCard'`
2. Outer: `bg-gray-50` → `bg-black`
3. Inner card: replace with `motion.div` + `GlassCard className="p-8"` pattern (same as LoginPage)
4. Heading: `text-gray-900` → `text-white`
5. Labels, inputs, button: apply identical dark patterns as LoginPage
6. Any back/link text: `text-gray-500` → `text-slate-400`, `hover:text-gray-700` → `hover:text-white`
7. Success/error message colors: keep red for errors, update success to `text-green-400` if needed

**ResetPasswordPage.tsx changes:** This page has 3 UI states (tokenError, success, form). Apply dark theme to all three states:
1. Add `motion` and `GlassCard` imports
2. Outer wrapper: `bg-gray-50` → `bg-black` (or whatever the current bg is)
3. All three rendered states (error card, success card, form card) must be wrapped in `motion.div` + `GlassCard` pattern with the same `initial={{ opacity: 0, y: 20 }}` entrance
4. Inputs: dark glass pattern
5. Primary button: gradient pattern
6. All text colors: apply dark equivalents (text-white for headings, text-slate-400 for secondary, text-slate-300 for labels)

CRITICAL: Do not change any logic, state management, API calls, or routing. Only className values.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Both files compile cleanly. Full test suite still passes. All 4 auth pages use bg-black outer + GlassCard inner + dark inputs + gradient button.</done>
</task>

</tasks>

<verification>
Run full suite: `cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm test -- --run`
- LoginPage.test.tsx: GREEN (bg-black assertion passes)
- LandingPage.test.tsx: still GREEN
- AdminPage.test.tsx: still GREEN
- AppNav.test.tsx: still GREEN
Run build: `cd /c/Users/raphg/Desktop/IA/ava2/frontend && npm run build` — zero TypeScript errors
</verification>

<success_criteria>
- All 4 auth pages (login, signup, forgot-password, reset-password) use bg-black as outer background
- Each page renders a GlassCard (or equivalent) as the centered card
- All form inputs use bg-white/5 border-white/10 dark glass pattern
- All primary submit buttons use from-blue-600 to-violet-600 gradient
- GoogleSignInButton uses border-white/20 text-white hover:bg-white/5
- LoginPage.test.tsx passes GREEN
- Full test suite GREEN, build clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-apply-the-style-of-the-front-page-to-the-rest-of-the-ui-of-the-entire-app/14-02-SUMMARY.md`
</output>
