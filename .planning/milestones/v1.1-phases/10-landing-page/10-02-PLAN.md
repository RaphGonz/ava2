---
phase: 10-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - frontend/src/components/landing/LandingHero.tsx
  - frontend/src/components/landing/LandingDualPromise.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
must_haves:
  truths:
    - "Hero section renders with English copy, split diagonal layout, Jarvis OS mockup, and a 'Create my Ava' link routing to /signup"
    - "DualPromise section renders with two GlassCards: 'Assistant Mode' (blue/cool) and 'Companion Mode' (warm/orange)"
    - "No French text or prohibited words appear in either component's output"
  artifacts:
    - path: "frontend/src/components/landing/LandingHero.tsx"
      provides: "Full-screen hero section with split diagonal background, Jarvis mockup, CTAs"
      exports: ["LandingHero"]
    - path: "frontend/src/components/landing/LandingDualPromise.tsx"
      provides: "Two-column GlassCard section showing Assistant Mode and Companion Mode"
      exports: ["LandingDualPromise"]
  key_links:
    - from: "frontend/src/components/landing/LandingHero.tsx"
      to: "/signup"
      via: "Link component from react-router-dom"
      pattern: "to=\"/signup\""
    - from: "frontend/src/components/landing/LandingDualPromise.tsx"
      to: "frontend/src/components/ui/GlassCard.tsx"
      via: "named import"
      pattern: "from.*ui/GlassCard"
---

<objective>
Port the Hero and DualPromise sections from the Figma export into standalone landing section components. Translate all French copy to English, fix the four known pitfalls (Math.random audio visualizer, Unsplash images, CTA as Link, prohibited copy), and keep the visual structure intact.

Purpose: These two sections form the top half of the landing page — hero grabs attention, DualPromise explains the product's dual-mode value proposition.
Output: LandingHero.tsx and LandingDualPromise.tsx, both usable as `<LandingHero />` and `<LandingDualPromise />` in the page assembly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-landing-page/10-CONTEXT.md
@.planning/phases/10-landing-page/10-RESEARCH.md
@.planning/phases/10-landing-page/10-01-SUMMARY.md

<interfaces>
<!-- Figma Hero source (adapt — do NOT copy verbatim): -->
<!-- Key structure: section.h-screen with two absolute background layers (blue clipped + violet/orange base) -->
<!-- Left content: motion.div with h1 + Jarvis OS mockup card -->
<!-- Right content: motion.div with h1 + mobile notification mockup -->
<!-- Bottom center: absolute CTAs (Create my Ava → /signup, Watch demo → no-op button) -->

<!-- PITFALLS TO FIX in Hero port: -->
<!-- 1. Math.random() in audio visualizer → use BAR_HEIGHTS constant: [18, 28, 14, 32, 20] -->
<!-- 2. Unsplash <img> tags → REMOVE; rely on gradient backgrounds already present -->
<!-- 3. French copy: "L'IA qui prends soin de votre énergie la journée." → "The AI that takes care of your day." -->
<!-- 4. "...et de votre intimité la nuit." → "...and keeps you company at night." (LAND-03: no "intimate") -->
<!-- 5. "Maintenant" → "Now"; "J'ai une surprise pour toi..." → "I have something for you..." -->
<!-- 6. "Créer mon AVA" → Link to="/signup" with text "Create my Ava" -->
<!-- 7. "Voir la démo" → plain <button> (no-op, not a Link) with text "Watch the demo" -->
<!-- 8. clip-path MUST be inline style: style={{ clipPath: 'polygon(0 0, 65% 0, 35% 100%, 0 100%)' }} -->

<!-- Figma DualPromise source (adapt): -->
<!-- Two GlassCards: variant="active-cool" (Brain icon) + variant="active-warm" (Heart icon) -->
<!-- PITFALLS TO FIX in DualPromise port: -->
<!-- 1. "Mode Assistant" → "Assistant Mode" -->
<!-- 2. "Mode Compagnon" → "Companion Mode" -->
<!-- 3. French paragraph text → translate per copy map in RESEARCH.md -->
<!-- 4. "Maintenir pour révéler" blurred button → translate to "Hold to reveal" OR remove; either is fine -->

<!-- GlassCard is available at: frontend/src/components/ui/GlassCard.tsx -->
<!-- Export: export function GlassCard({ variant, children, className, ...props }) -->

<!-- CTA pattern (from RESEARCH.md): -->
<!-- motion.div wrapping Link — NOT motion.button with href -->
<!-- import { Link } from 'react-router-dom' -->
<!-- import { motion } from 'motion/react' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Hero section</name>
  <files>frontend/src/components/landing/LandingHero.tsx</files>
  <action>
Create `frontend/src/components/landing/LandingHero.tsx`. Port from `.planning/assets/figma_frames/src/app/components/Hero.tsx` with these mandatory changes:

**Background layers:**
- Keep the blue clipped left layer with `style={{ clipPath: 'polygon(0 0, 65% 0, 35% 100%, 0 100%)' }}` as inline style (NOT a Tailwind class — Pitfall 5)
- REMOVE the two `<img>` tags with Unsplash URLs — keep only the gradient backgrounds (`bg-blue-950`, `bg-gradient-to-br from-violet-900 to-orange-900`)
- The `<div className="absolute inset-0 bg-gradient-to-r from-blue-900/80 to-transparent" />` overlay stays

**Left content (Jarvis side):**
- h1 text: "The AI that takes care of your day." (was: "L'IA qui prends soin de votre énergie la journée.")
- Keep the Jarvis OS mockup card unchanged (English text already in mockup: "Deep Work: Q3 Strategy", "Client Sync: Alpha Corp", etc.)

**Right content (Companion side):**
- h1 text: "...and keeps you company at night." (was: "...et de votre intimité la nuit." — "intimate" is prohibited per LAND-03)
- Mobile notification mockup: change "Maintenant" → "Now"; change "J'ai une surprise pour toi..." → "I have something for you..."
- Audio visualizer: REPLACE `{[...Array(5)].map((_, i) => <div key={i} style={{ height: Math.random() * 24 + 8 + 'px' }} />)}` with pre-computed heights to avoid React strict mode issues (Pitfall 8):
```typescript
const BAR_HEIGHTS = [18, 28, 14, 32, 20]
// then in JSX:
{BAR_HEIGHTS.map((height, i) => (
  <div key={i} className="w-1 bg-white/50 rounded-full animate-pulse" style={{ height: `${height}px`, animationDelay: `${i * 0.1}s` }} />
))}
```

**CTAs (center bottom):**
- Primary: Replace `<motion.button>Créer mon AVA</motion.button>` with:
```tsx
<motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
  <Link
    to="/signup"
    className="px-8 py-4 rounded-full bg-gradient-to-r from-blue-600 via-violet-600 to-orange-600 text-white font-bold text-lg shadow-[0_0_30px_-5px_rgba(79,70,229,0.5)] hover:shadow-[0_0_50px_-5px_rgba(79,70,229,0.7)] transition-all flex items-center gap-2"
  >
    Create my Ava <ArrowRight className="w-5 h-5" />
  </Link>
</motion.div>
```
- Secondary: Plain `<button>` (NOT a Link — Watch demo is no-op per deferred decisions):
```tsx
<button
  className="px-8 py-4 rounded-full bg-white/5 backdrop-blur-sm border border-white/10 text-white font-medium text-lg hover:bg-white/10 transition-all flex items-center gap-2"
  onClick={() => {}}
>
  <Play className="w-4 h-4 fill-current" /> Watch the demo
</button>
```

**Mobile responsiveness:** The Figma already has `md:flex-row` pattern. Add `hidden md:flex-col` on right content for small screens so hero is not broken on mobile — stack both content panels vertically on mobile (`flex-col` default, `md:flex-row`).

**Exports:** `export function LandingHero()`

**Imports needed:** `motion` from `'motion/react'`, `ArrowRight, Play` from `'lucide-react'`, `Link` from `'react-router-dom'`, `useState, useEffect` from `'react'` (for TypingText).

Keep the `TypingText` helper as-is from the Figma (it is already in the Figma source and works correctly).
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2 && grep -c "Create my Ava" frontend/src/components/landing/LandingHero.tsx && grep -c "to=\"/signup\"" frontend/src/components/landing/LandingHero.tsx && grep -c "BAR_HEIGHTS" frontend/src/components/landing/LandingHero.tsx && grep -c "intimate\|intimité\|Créer\|Maintenant\b" frontend/src/components/landing/LandingHero.tsx | grep "^0$" && echo "Hero OK"</automated>
  </verify>
  <done>LandingHero.tsx exists, exports LandingHero, contains "Create my Ava" text, has a Link to="/signup", uses BAR_HEIGHTS constant (no Math.random), contains no prohibited/French words, and clip-path is set via inline style prop.</done>
</task>

<task type="auto">
  <name>Task 2: Port DualPromise section</name>
  <files>frontend/src/components/landing/LandingDualPromise.tsx</files>
  <action>
Create `frontend/src/components/landing/LandingDualPromise.tsx`. Port from `.planning/assets/figma_frames/src/app/components/DualPromise.tsx` with these mandatory changes:

**Section heading:** There is no section heading in the Figma DualPromise. Keep the section as-is (no h2 needed).

**Assistant Mode card (active-cool variant):**
- Card heading: "Mode Assistant" → "Assistant Mode"
- Paragraph: "Une intelligence analytique pure pour structurer votre chaos. Gestion d'agenda, emails, synthèse de documents. Elle ne dort jamais, ne juge jamais." → "Pure analytical intelligence to organise your day. Calendar management, email drafts, document summaries. Always available. Never judgmental."
- Visual (mini agenda skeleton bars): Keep unchanged

**Companion Mode card (active-warm variant):**
- Card heading: "Mode Compagnon" → "Companion Mode"
- Paragraph: "Plus qu'une IA, une présence. Connexion émotionnelle, écoute active et partage multimédia privé. Apprenez à lâcher prise." → "More than an AI, a presence. Emotional connection, active listening and private media sharing. Learn to unwind."
- Blurred button: Change "Maintenir pour révéler" → "Hold to reveal" (decorative only, keep as-is otherwise)

**Import paths:** GlassCard is at `'../ui/GlassCard'` (relative from `components/landing/` to `components/ui/`). Import motion from `'motion/react'`.

**Exports:** `export function LandingDualPromise()`

**Mobile:** Cards are already `flex-col md:flex-row` via `flex flex-col md:flex-row gap-8`. Keep this pattern.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/IA/ava2 && grep -c "Assistant Mode" frontend/src/components/landing/LandingDualPromise.tsx && grep -c "Companion Mode" frontend/src/components/landing/LandingDualPromise.tsx && grep "Mode Assistant\|Mode Compagnon\|intimité\|Connexion" frontend/src/components/landing/LandingDualPromise.tsx | wc -l | grep "^0$" && echo "DualPromise OK"</automated>
  </verify>
  <done>LandingDualPromise.tsx exists, exports LandingDualPromise, shows "Assistant Mode" and "Companion Mode" headings in English, imports GlassCard from correct relative path, contains no French text or prohibited words.</done>
</task>

</tasks>

<verification>
- `ls frontend/src/components/landing/LandingHero.tsx` → exists
- `ls frontend/src/components/landing/LandingDualPromise.tsx` → exists
- `grep "to=\"/signup\"" frontend/src/components/landing/LandingHero.tsx` → found (CTA is a Link)
- `grep "Math.random" frontend/src/components/landing/LandingHero.tsx` → NOT found (pre-computed heights)
- `grep "Unsplash\|unsplash" frontend/src/components/landing/LandingHero.tsx` → NOT found
- `grep -i "intimate\|explicit\|nsfw\|18+" frontend/src/components/landing/LandingHero.tsx` → NOT found
- `grep -i "intimate\|explicit\|nsfw\|18+" frontend/src/components/landing/LandingDualPromise.tsx` → NOT found
</verification>

<success_criteria>
- Hero section component renders the full-screen split layout with English copy, pre-computed audio visualizer bars, "Create my Ava" Link to /signup, "Watch the demo" no-op button
- DualPromise component renders two glass cards with "Assistant Mode" and "Companion Mode" in English
- Neither component contains French text or Stripe-prohibited language
- TypeScript compiles: `cd frontend && npm run build` has no errors from these two files
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-02-SUMMARY.md`
</output>
