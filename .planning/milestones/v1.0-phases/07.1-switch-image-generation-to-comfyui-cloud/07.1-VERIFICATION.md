---
phase: 07.1-switch-image-generation-to-comfyui-cloud
verified: 2026-02-25T15:30:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 07.1: Switch Image Generation to ComfyUI Cloud — Verification Report

**Phase Goal:** Replace Replicate FLUX as the image generation backend with ComfyUI Cloud (cloud.comfy.org) using Qwen models. The ComfyUIProvider skeleton, workflow JSON files, and processor wiring are already implemented. This phase verifies API compatibility, fixes remaining issues (seed randomization, any schema mismatches), validates end-to-end pipeline correctness, and removes all Replicate dependencies from the active code path.
**Verified:** 2026-02-25T15:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                             | Status     | Evidence                                                                                                                |
|----|-------------------------------------------------------------------------------------------------------------------|------------|-------------------------------------------------------------------------------------------------------------------------|
| 1  | Image generation completes end-to-end via the 4-step ComfyUI Cloud flow (submit → poll → history → download)     | VERIFIED   | `_poll_and_download` calls `_fetch_history_and_download` on `status=completed`; `_fetch_history_and_download` calls `/api/history_v2/{prompt_id}` then `/api/view`; `test_t2i_generate_returns_image_bytes` passes |
| 2  | Each generate() call uses a different random seed — repeated calls produce different images                        | VERIFIED   | `random.randint(0, 2**32 - 1)` in both `_build_t2i` (node `86:3`) and `_build_i2i` (node `89:65`); `test_seed_is_randomized_across_calls` passes |
| 3  | ComfyUIProvider satisfies the updated ImageProvider Protocol, including `reference_image_url` parameter            | VERIFIED   | `ImageProvider.generate` Protocol signature includes `reference_image_url: str | None = None`; `test_protocol_compliance` (`isinstance(ComfyUIProvider(), ImageProvider)`) passes |
| 4  | Unit tests pass without a live COMFYUI_API_KEY                                                                    | VERIFIED   | All 5 tests in `test_comfyui_provider.py` pass using mocked `httpx.AsyncClient` and `patch("app.config.settings.comfyui_api_key", "test-key")`; confirmed via live test run: `5 passed in 0.34s` |
| 5  | New developers reading `.env.example` are directed to get a ComfyUI Cloud API key, not a Replicate key            | VERIFIED   | `.env.example` has `COMFYUI_API_KEY=` and `COMFYUI_BASE_URL=https://cloud.comfy.org` under `# --- ComfyUI Cloud (required for image generation) ---`; `REPLICATE_API_TOKEN` is absent |
| 6  | The `replicate` package is removed from `requirements.txt`                                                        | VERIFIED   | `requirements.txt` contains no `replicate` line; grep confirms absence |
| 7  | `processor.py` module docstring accurately describes the ComfyUI Cloud pipeline steps, not Replicate FLUX         | VERIFIED   | Docstring describes "ComfyUI Cloud API (image-to-image if reference exists, else text-to-image)" with "4-step flow: POST /api/prompt → poll /api/job/{id}/status → GET /api/history_v2/{id} → GET /api/view (download)" |
| 8  | `config.py` makes clear that `replicate_api_token` is legacy/unused                                               | VERIFIED   | Line 33: `# Image generation (Replicate — legacy, unused. ComfyUI Cloud is the active provider.)` |
| 9  | Full backend test suite passes with no regressions                                                                | VERIFIED   | `52 passed, 25 warnings in 13.03s` — all tests pass |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact                                                        | Expected                                                                 | Status     | Details                                                                                                         |
|-----------------------------------------------------------------|--------------------------------------------------------------------------|------------|-----------------------------------------------------------------------------------------------------------------|
| `backend/app/services/image/comfyui_provider.py`                | 4-step flow; `_fetch_history_and_download` with 3-attempt retry; seeds randomized | VERIFIED   | 228 lines; `_fetch_history_and_download` exists (line 165); `random.randint` in `_build_t2i` (line 83) and `_build_i2i` (line 94); `history_v2` in URL at line 179 |
| `backend/app/services/image/base.py`                            | `ImageProvider` Protocol with `reference_image_url: str | None = None`   | VERIFIED   | Line 29: `reference_image_url: str | None = None` in Protocol signature; `@runtime_checkable` present          |
| `backend/tests/test_comfyui_provider.py`                        | 5 unit tests covering t2i, i2i, seed randomization, Protocol, retry      | VERIFIED   | 264 lines; 5 test functions; all 5 pass without live API key                                                    |
| `backend/requirements.txt`                                      | `replicate` line removed                                                 | VERIFIED   | No `replicate` entry; 24 packages listed                                                                        |
| `backend/.env.example`                                          | ComfyUI Cloud section with `COMFYUI_API_KEY` and `COMFYUI_BASE_URL`      | VERIFIED   | Section present; `REPLICATE_API_TOKEN` absent                                                                   |
| `backend/app/services/jobs/processor.py`                        | Docstring describes ComfyUI Cloud 4-step pipeline                        | VERIFIED   | Line 10-19: ComfyUI Cloud pipeline described; `from app.services.image.comfyui_provider import ComfyUIProvider` at line 23; `_image_provider = ComfyUIProvider()` at line 33 |
| `backend/app/config.py`                                         | `replicate_api_token` comment updated to legacy/unused                   | VERIFIED   | Line 33: comment explicitly states "legacy, unused. ComfyUI Cloud is the active provider."                      |
| `backend/app/services/image/workflows/text_to_image.json`       | Workflow JSON for t2i path                                               | VERIFIED   | File exists                                                                                                     |
| `backend/app/services/image/workflows/image_to_image.json`      | Workflow JSON for i2i path                                               | VERIFIED   | File exists                                                                                                     |

---

### Key Link Verification

| From                                       | To                                    | Via                                           | Status   | Details                                                                                          |
|--------------------------------------------|---------------------------------------|-----------------------------------------------|----------|--------------------------------------------------------------------------------------------------|
| `comfyui_provider.py`                      | `/api/history_v2/{prompt_id}`         | `_fetch_history_and_download` method          | WIRED    | Line 179: `f"{settings.comfyui_base_url}/api/history_v2/{prompt_id}"`                           |
| `comfyui_provider.py`                      | `random.randint`                      | `_build_t2i` and `_build_i2i`                 | WIRED    | Line 14: `import random`; line 83: `random.randint(0, 2**32 - 1)`; line 94: `random.randint(0, 2**32 - 1)` |
| `backend/.env.example`                     | `backend/app/config.py`               | `COMFYUI_API_KEY` env var → `comfyui_api_key` | WIRED    | `.env.example` has `COMFYUI_API_KEY=`; `config.py` line 37: `comfyui_api_key: str = ""`        |
| `processor.py`                             | `ComfyUIProvider`                     | `_image_provider = ComfyUIProvider()`         | WIRED    | Line 23: import; line 33: instantiation; line 112: `await _image_provider.generate(...)`        |

---

### Requirements Coverage

No formal requirement IDs were assigned to this phase (decimal/inserted phase). All success criteria from PLAN frontmatter are covered above.

---

### Anti-Patterns Found

| File                                                     | Line | Pattern                                    | Severity | Impact   |
|----------------------------------------------------------|------|--------------------------------------------|----------|----------|
| `backend/app/services/image/comfyui_provider.py`         | 8    | Module docstring still references `/api/view` without `history_v2` in the summary line | Info | Cosmetic only — implementation is correct |

No blockers found. No TODO/FIXME/placeholder comments. No stub implementations. No empty handlers.

Note: `replicate_provider.py` is intentionally kept as dead-code emergency fallback (as documented in RESEARCH.md decision). It uses `httpx` directly (not the `replicate` package), so it carries no import risk. The `replicate` package itself is absent from `requirements.txt`.

---

### Human Verification Required

None. All behavioral claims are verified programmatically via the test suite and static code analysis.

---

### Commit Verification

All four commits documented in SUMMARYs are confirmed present in git history:

- `b350008` — `test(07.1-01): add failing tests for ComfyUIProvider correct API flow`
- `9b73b0b` — `feat(07.1-01): fix ComfyUIProvider API flow + seed randomization + Protocol update`
- `d19c417` — `chore(07.1-02): remove replicate from requirements.txt; update .env.example to ComfyUI Cloud`
- `8e30125` — `fix(07.1-02): update processor.py docstring, config.py comment; fix ComfyUI 4-step flow`

---

### Deviation from Plan (Noted, Not Blocking)

The PLAN for 07.1-01 specified that `_download_output` should be removed entirely and its logic inlined in `_fetch_history_and_download`. The implementation instead kept `_download_output` as a private helper called by `_fetch_history_and_download`. This is documented in SUMMARY-01 under "Decisions Made" as a deliberate choice to preserve single-responsibility separation. The behavior is functionally identical and all tests pass — this is not a gap.

---

### Summary

Phase 07.1 goal is fully achieved. The ComfyUI Cloud provider is:

1. **Correctly implemented** — 4-step API flow with history_v2 retrieval and 3-attempt retry for the output persistence race condition
2. **Correctly seeded** — `random.randint(0, 2**32 - 1)` injected per-call in both t2i and i2i workflow builders
3. **Protocol-compliant** — `ComfyUIProvider` satisfies the updated `ImageProvider` Protocol including `reference_image_url`
4. **Fully tested** — 5 unit tests with mocked httpx, all passing without a live API key
5. **Clean of Replicate** — `replicate` package removed from `requirements.txt`; `.env.example` points to ComfyUI Cloud; no `import replicate` anywhere in `app/`; config and processor docstrings accurately reflect the active provider
6. **Zero regressions** — 52/52 backend tests pass

---

_Verified: 2026-02-25T15:30:00Z_
_Verifier: Claude (gsd-verifier)_
